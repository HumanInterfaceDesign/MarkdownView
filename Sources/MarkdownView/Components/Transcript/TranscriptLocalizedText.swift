import Foundation

enum TranscriptLocalizedText {
    static let showMore = NSLocalizedString(
        "Show more",
        bundle: .module,
        comment: "Button revealing the full transcript below an audio message"
    )
    static let transcript = NSLocalizedString(
        "Transcript",
        bundle: .module,
        comment: "Title of the full-transcript detail screen"
    )
}
